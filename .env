# Đã chuyển sang Supabase (Postgres). DATABASE_URL và DIRECT_URL là secret thật
# (có mật khẩu DB) nên đặt trong .env.local (không commit), KHÔNG đặt ở đây.
# Xem hướng dẫn/format trong .env.example.
